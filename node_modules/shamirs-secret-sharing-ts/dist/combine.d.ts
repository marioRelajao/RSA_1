/// <reference types="node" />
/**
 * Reconstruct a secret from a distinct set of shares.
 * @public
 * @param {Array<String|Buffer>} shares
 * @return {Buffer}
 */
declare function combine(shares: any): Buffer;
export { combine };
